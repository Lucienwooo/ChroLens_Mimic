# 🚀 快速開始

## 打包程式

### 方法 1: PowerShell (推薦,最簡單)
```powershell
.\build.ps1
```

### 方法 2: Python
```powershell
python build.py
```

### 方法 3: Python (完整清理)
```powershell
python build.py --clean
```

## 輸出位置

打包完成後,程式位於:
```
dist\ChroLens_Mimic\ChroLens_Mimic.exe
```

## 檔案結構

```
dist\ChroLens_Mimic\
├── ChroLens_Mimic.exe       主程式
├── version2.6.5.txt          版本資訊
├── _internal\                程式核心
├── scripts\                  使用者腳本 (自動保留)
├── user_config.json          使用者設定 (自動保留)
└── backup\                   舊版本備份
```

## 發布到 GitHub

1. 建立 Release,標籤為 `v2.6.5`
2. 壓縮 `dist\ChroLens_Mimic` 整個資料夾為 `ChroLens_Mimic.zip`
3. 上傳 ZIP 到 Release
4. 填寫更新說明

## 更多資訊

詳細說明請參考 `BUILD_README.md`
